// 쓰는 모델을 한곳에 모아 둔다.
//
// 예전엔 파일마다 모델 이름을 적어 두었는데, 현장이야기 두 곳이
// "claude-sonnet-4-20250514" 로 날짜까지 박힌 채 남아 있었다. 그 모델이 은퇴한
// 뒤에도 아무도 몰랐고, 자동 생성을 켰다면 매일 조용히 실패했을 것이다.
//
// 날짜를 붙이지 않은 이름을 쓴다. 같은 세대 안에서 개선판이 나오면 그건 저절로
// 따라간다. 다만 세대가 바뀌면(하이쿠 4.5 → 하이쿠 5) 이름이 달라지므로 아래
// 한 줄만 고치면 된다. 급하면 환경변수로 배포 없이 바꿀 수도 있다.

/** 공고 불러오기(외부공고 파싱). 매일 여러 건 눌러서 요금이 가장 많이 나가는 곳. */
export const PARSE_MODEL = process.env.PARSE_MODEL || "claude-haiku-4-5";

/** 글을 짓거나 다듬는 곳 — 큐레이션·뉴스레터·현장이야기. */
export const WRITING_MODEL = process.env.WRITING_MODEL || "claude-haiku-4-5";

/** 구직자가 직접 누르는 곳 — 자기소개서 초안·맞춤법. 사람 손에 호출 수가
 *  달려 있어 값싼 쪽으로 못 박아 둔다(글 짓는 다른 곳과 따로 고를 수 있게). */
export const ASSIST_MODEL = process.env.ASSIST_MODEL || "claude-haiku-4-5";
