/** 문의 유형 — 접수하는 폼과 관리자 필터가 같은 목록을 본다.
 *  세 군데(1:1 폼·접수 API·관리자)에 같은 배열이 흩어져 있었다.
 *  as const 로 굳히지 않는다 — 고른 값을 담는 자리가 여섯 낱말 중
 *  하나로 좁아져, 「전체」를 담지 못한다.
 *
 *  기업회원으로 들어온 사람에게 "이력서/프로필"이 첫 줄로 뜨고, 정작
 *  기업이 실제로 겪는 일(공고 등록·지원자 관리·결제)은 "기업회원" 한
 *  칸에 뭉뚱그려져 있었다. 개인·기업 목록을 나누고, 관리자 필터·API
 *  검증은 둘을 합친 전체를 본다. */
export const 개인문의유형: string[] = [
  "계정/로그인",
  "이력서/프로필",
  "채용공고/지원",
  "신고/불편사항",
  "기타",
];

export const 기업문의유형: string[] = [
  "계정/로그인",
  "공고 등록·관리",
  "지원자·인재",
  "상품·결제",
  "신고/불편사항",
  "기타",
];

/** 관리자 필터·API 검증용 전체 목록. */
export const 문의유형: string[] = Array.from(new Set([...개인문의유형, ...기업문의유형]));

/** 사업문의(제휴·광고·기타)의 갈래. 폼이 type 으로 넣는 값과 같다. */
export const 사업문의유형: string[] = ["광고", "제휴", "기타"];
