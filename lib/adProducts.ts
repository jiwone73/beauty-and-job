/**
 * 배너광고 상품 — 공고가 아니라 **자리**를 파는 것들.
 *
 * 고르는 기준이 「어디에 뜨는가」 하나뿐이라 화면으로 나눈다. 브랜드를 알리러
 * 온 곳은 메인만, 공고를 밀러 온 곳은 채용공고 페이지만 보면 된다.
 *
 * 짜임은 채용공고 상품과 같게 둔다 — 안내 화면에서 카드로 고르고, 「자세히
 * 보기」로 들어가면 상품 내용·기간·값·노출 자리·유의사항이 한 장에 있다.
 */

export type 광고면 = "MAIN" | "JOBS";

export const 면이름: Record<광고면, string> = {
  MAIN: "메인페이지 노출",
  JOBS: "공고페이지 노출",
};
export const 면설명: Record<광고면, string> = {
  MAIN: "사이트에 들어온 사람 모두가 첫 화면에서 봅니다. 가장 많은 사람에게 닿는 자리입니다.",
  JOBS: "일자리를 찾는 중인 사람에게만 뜹니다. 보는 사람을 좁혀서 닿는 자리입니다.",
};

/**
 * 카드에 적는 것 — 낱개 상품 이름을 늘어놓지 않는다.
 *
 * 카드에서 정하는 것은 「어느 화면에 걸까」 하나다. 그 화면 안에 상품이 몇
 * 가지인지는 자세히 보기가 말한다 — 카드에 다 적으면 고르기도 전에 낱개를
 * 견주게 되고, 갈래마다 개수가 달라 단추 자리도 어긋난다.
 *
 * 두 칸의 줄 수를 맞춘다. 한쪽만 길면 카드 높이가 갈린다.
 */
export const 면요약: Record<광고면, string[]> = {
  MAIN: [
    "사이트에 들어온 사람 전원에게 노출",
    "첫 화면 최상단 배너 (1320 × 190)",
    "클릭 시 지정하신 주소로 이동",
    "기간 종료 후 노출·클릭 리포트",
  ],
  JOBS: [
    "일자리를 찾는 중인 사람에게 노출",
    "채용공고 목록 최상단 배너 (1320 × 190)",
    "직군을 골라 그 직군에만 걸 수 있음",
    "기간 종료 후 노출·클릭 리포트",
  ],
};

/** 배너 기간. 채용공고 상품과 달리 두 주 단위가 있다 — 광고는 소재를 바꿔 가며 건다. */
export const 광고기간 = [7, 14, 30] as const;
export type 광고기간 = (typeof 광고기간)[number];

export type 광고상품 = {
  id: string;
  면: 광고면;
  name: string;
  /** 어디에 뜨는가 — 이름 바로 아래 적는다 */
  자리: string;
  한줄: string;
  /**
   * 기간별 값. null 은 「협의」다.
   *
   * 메인 배너 값은 /company/ads 가 쓰던 것을 그대로 옮겼다. 공고페이지 두 자리는
   * 값을 정한 적이 없어 협의로 둔다 — 없는 값을 지어내 화면에 걸면 그것이 곧
   * 약속이 된다.
   *
   * **부가세 포함가**다. 채용공고 상품도, 사람인·잡코리아·헤어인잡도 다 포함가로
   * 적는다(헤어인잡의 77,000·88,000·99,000 은 7·8·9만 원에 부가세를 붙인 값이다).
   * 한 사이트 안에서 어떤 상품은 포함, 어떤 상품은 별도면 그 차이를 사는 쪽이
   * 계산해야 한다.
   */
  가격: Record<광고기간, number | null>;
  /** 상품 내용 표 */
  사양: { 항목: string; 값: string }[];
};

export const 광고상품들: 광고상품[] = [
  {
    id: "main-banner",
    면: "MAIN",
    name: "메인 AD 배너",
    자리: "메인 페이지 최상단",
    한줄: "사이트에 들어온 사람 모두가 첫 화면에서 본다",
    가격: { 7: 200000, 14: 350000, 30: 600000 },
    사양: [
      { 항목: "노출 위치", 값: "메인 페이지 최상단 (머리줄 바로 아래)" },
      { 항목: "노출 대상", 값: "사이트 방문자 전원" },
      { 항목: "소재 크기", 값: "1320 x 190 (PC·모바일 같은 소재)" },
      { 항목: "링크", 값: "클릭 시 지정하신 주소로 이동" },
      { 항목: "소재 준비", 값: "직접 주시거나 제작 대행(비용 별도 협의)" },
      { 항목: "리포트", 값: "기간이 끝나면 노출 수·클릭 수를 보내 드립니다" },
    ],
  },
  {
    id: "jobs-banner",
    면: "JOBS",
    name: "채용공고 페이지 배너",
    자리: "채용공고 목록 최상단 (직군 미선택)",
    한줄: "일자리를 찾는 사람이 목록을 열면 맨 위에서 본다",
    가격: { 7: null, 14: null, 30: null },
    사양: [
      { 항목: "노출 위치", 값: "채용공고 목록 최상단 (머리줄 바로 아래)" },
      { 항목: "노출 대상", 값: "채용공고를 보러 온 사람 전원" },
      { 항목: "소재 크기", 값: "1320 x 190 (PC·모바일 같은 소재)" },
      { 항목: "링크", 값: "클릭 시 지정하신 주소로 이동" },
      { 항목: "소재 준비", 값: "직접 주시거나 제작 대행(비용 별도 협의)" },
      { 항목: "리포트", 값: "기간이 끝나면 노출 수·클릭 수를 보내 드립니다" },
    ],
  },
  {
    id: "jobs-group-banner",
    면: "JOBS",
    name: "직군별 배너",
    자리: "채용공고 목록 최상단 (그 직군을 고른 사람에게만)",
    한줄: "헤어를 보는 사람에게 헤어 광고를 건다",
    가격: { 7: null, 14: null, 30: null },
    사양: [
      { 항목: "노출 위치", 값: "채용공고 목록 최상단 (직군 선택 시)" },
      { 항목: "노출 대상", 값: "고르신 직군을 선택한 사람에게만" },
      { 항목: "고를 수 있는 직군", 값: "매장 10묶음 · 오피스 8묶음 중 하나" },
      { 항목: "소재 크기", 값: "1320 x 190 (PC·모바일 같은 소재)" },
      { 항목: "링크", 값: "클릭 시 지정하신 주소로 이동" },
      { 항목: "리포트", 값: "기간이 끝나면 노출 수·클릭 수를 보내 드립니다" },
    ],
  },
];

/** 그 화면에 걸 수 있는 상품들 */
export const 면상품 = (면: 광고면) => 광고상품들.filter((a) => a.면 === 면);
/** id 로 하나 */
export const 광고하나 = (id: string) => 광고상품들.find((a) => a.id === id) ?? null;

/** 예전 이름 — 아직 /company/ads 가 쓰고 있다. */
export const 광고상품 = 광고상품들;
