# 상용화 전에 반드시 확인할 것

지금은 사장님 혼자 쓰는 상태라 문제가 없지만, 실제 사용자를 받기 전에는
하나씩 짚어야 하는 것들이다. 대화에서 나온 판단이 세션이 끝나면 사라지므로
여기에 남긴다. 처리한 항목은 지우지 말고 `~~취소선~~`으로 남겨 두면
왜 그렇게 했는지 나중에 알 수 있다.

---

## 1. 개인정보 — 인재검색이 연락처를 그대로 보여준다

**지금 상태**: 기업이 인재검색에서 **지원하지 않은 구직자의 전화번호·이메일·SNS**를
아무 동의 절차 없이 볼 수 있다. (`app/api/company/talent/route.ts`)

**왜 문제인가**
- 구직자는 "지원하려고" 가입했지, 모든 매장이 자기 번호를 열람하라고 가입한 것이 아니다.
  수집 목적 외 제공에 해당할 수 있다.
- 약관에 그 동의가 없다. 지금 있는 것은 넷뿐 — 이용약관 / 개인정보 처리방침(필수),
  마케팅 수신 / 추천 공고 알림(선택).
- 매장 연락처는 구직자에게 가리면서(`lib/hideContacts.ts`) 반대쪽은 열려 있어 규칙이
  한쪽으로만 적용돼 있다.
- 지원도 안 했는데 모르는 매장에서 전화가 오면 구직자가 이력서를 내린다.

**권하는 구조**
- 인재검색 목록에는 이름·경력·직군·지역·포트폴리오 사진까지만.
- 매장이 관심 있으면 `제안하기` → 뷰티워크를 통해 알림 → **구직자가 수락하면 연락처 공개**.
- 나중에 유료화한다면 이 자리다(제안 건수 또는 열람권).

**한시 무료 오픈은 권하지 않는다.** 한 번 열면 매장이 무료 열람에 익숙해져 닫을 때
이탈하고, 그때는 이미 번호를 다 가져간 뒤다. 무엇보다 동의 문제는 기간과 무관하다.

---

## 2. 개발용 인증번호가 화면에 찍힌다

`dev_code`가 있으면 인증번호를 화면에 그대로 보여준다. 문자 발송이 실패해도
가입이 되도록 둔 폴백인데, 남겨 두면 아무나 남의 번호로 가입할 수 있다.

- `app/signup/email/page.tsx`
- `app/profile/page.tsx` (이메일·전화 변경 두 곳)

---

## 3. 옛 이름 '하이어스'가 남아 있다

약관·소개 등에 이전 서비스명이 남아 있다. 법적 문서라 이름이 다르면 효력을 다툴 여지가 있다.

- `app/page.tsx`
- `app/about/page.tsx`
- `app/support/privacy/page.tsx`
- `app/support/terms/page.tsx`

---

## 4. 약관 시행일이 비어 있다

`app/support/privacy/page.tsx` 의 "제1조 시행일" 등. 실제 시행일을 넣어야 한다.

---

## 5. 푸터에 사업자정보가 없다

상호·대표자·사업자등록번호·통신판매업신고번호·주소·연락처는 전자상거래법상 표시 의무다.

---

## 6. 샘플 데이터가 실제 데이터와 섞여 있다

사용자 105명 중 100명이 샘플이다(`users.is_sample`). 공고·기업도 마찬가지.
매일 도는 크론이 샘플을 갱신하고 있다 — `refreshSampleData`,
`app/api/cron/generate-story/route.ts` 의 ⚠️ 표시 블록.

실사용자를 받기 전에 걷어내야 통계·목록이 사실을 말한다.

---

## 7. 검색엔진 색인이 막혀 있다

`app/robots.ts` 가 전부 `disallow`. 열 때 풀어야 한다. 도메인도 함께 정해야 한다.

---

## 8. 현장이야기 크론이 검수 없이 바로 게시한다

관리자 화면에서 만들면 `pending`(승인 후 게시)인데, 크론은 `published`로 바로 올린다.
자동 생성 토글(`app_settings.story_autogen`)이 지금은 꺼져 있어 문제가 없지만,
켜는 순간 AI가 쓴 글이 아무도 안 읽은 채 커뮤니티에 올라간다.

`app/api/cron/generate-story/route.ts`

---

## 9. 지원자 알림 문자 (미착수)

지원했을 때·열람됐을 때 구직자에게 알리는 문자. 아직 만들지 않았다.
