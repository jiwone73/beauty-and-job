-- 기본 자기소개서.
--
-- 지원할 때마다 빈 칸에서 다시 쓰게 하면 대부분 빈칸으로 낸다. 한 벌을 이력서에
-- 담아 두고 지원할 때 불러다 두세 줄 고치는 것이 실제로 쓰이는 방식이다.
-- 필수는 아니다 — 미용은 자소서를 길게 쓰는 문화가 아니고, 이력서 등록 화면에
-- 큰 빈 칸이 필수로 서 있으면 거기서 이탈한다. 한줄소개(intro)가 이미 그 자리를
-- 맡고 있다.
--
-- 기업에게는 공고를 올린 곳에만 열린다(핵심역량·연락처·작업물과 같은 잠금).
ALTER TABLE user_profiles ADD COLUMN IF NOT EXISTS cover_letter TEXT;
