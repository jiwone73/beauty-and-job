-- 사업문의에도 제목을 받는다.
--
-- 1:1 문의(inquiries)는 처음부터 subject 가 있었는데 사업문의만 없어, 관리자
-- 메일함에서 회사명만 보고 열어봐야 무슨 일인지 알 수 있었다. 같은 메일함이면
-- 같은 것이 보여야 한다.
--
-- 이미 들어온 문의는 제목이 없다. 상품 문의는 상품명(product)이 제목 노릇을
-- 하므로 화면에서 그것으로 대신 채우고, 그 밖은 「(제목 없음)」으로 둔다 —
-- 내용 앞부분을 잘라 제목인 척 채우지 않는다.
ALTER TABLE ad_inquiries ADD COLUMN IF NOT EXISTS subject text;
